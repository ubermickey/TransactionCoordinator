# =============================================================================
# Transaction Coordinator — Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.
# =============================================================================

# --- Claude API (document extraction, AI analysis) ---
ANTHROPIC_API_KEY=sk-ant-...

# --- Google Workspace ---
# OAuth credentials JSON file path (download from Google Cloud Console)
GOOGLE_CREDENTIALS_FILE=credentials.json
# Token storage path (auto-generated after first OAuth flow)
GOOGLE_TOKEN_FILE=token.json
# Gmail alias to send emails FROM
GMAIL_SEND_AS_EMAIL=transactions@yourdomain.com
# Agent's personal email (for receiving review notifications)
AGENT_EMAIL=you@yourdomain.com

# --- DocuSign ---
DOCUSIGN_INTEGRATION_KEY=your-integration-key
DOCUSIGN_SECRET_KEY=your-secret-key
DOCUSIGN_ACCOUNT_ID=your-account-id
DOCUSIGN_BASE_URL=https://demo.docusign.net/restapi
# Set to https://na4.docusign.net/restapi for production
DOCUSIGN_OAUTH_BASE=https://account-d.docusign.com
# Set to https://account.docusign.com for production

# --- Push Notifications ---
# Choose one or both. Leave blank to disable.

# Pushover (https://pushover.net — $5 one-time per platform)
PUSHOVER_USER_KEY=your-pushover-user-key
PUSHOVER_API_TOKEN=your-pushover-app-token

# ntfy (https://ntfy.sh — free, self-hostable)
NTFY_TOPIC=your-private-topic-name
NTFY_SERVER=https://ntfy.sh

# --- Application Settings ---
# Where transaction data is stored locally
DATA_DIR=./data
# Timezone for deadline calculations
TIMEZONE=America/Los_Angeles
# Daily digest time (24hr format)
DAILY_DIGEST_TIME=08:00
# Path to jurisdiction YAML rules
JURISDICTIONS_DIR=./jurisdictions
# Path to workflow YAML definitions
WORKFLOW_DIR=./workflow
